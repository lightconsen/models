# Provider review checklist

50 providers — 26 checked against their vendors, 24 seeded on 2026-09-22 from models.dev and pending verification. Tick one when its data has been checked against what the vendor itself publishes — the prices, and the things a price depends on:

- every live model is listed, and no retired one is — where the vendor's own page
  enumerates its lineup, which is most of them. An aggregator routing hundreds
  does not, so the entry carries the ones it is known for and the notes say which
  rule picked them (`openrouter` uses the vendor's own usage ranking)
- `in` / `out` / `cache_read` match the vendor's own table, in the currency the vendor bills in
- any time-of-day *or length-dependent* pricing is recorded, not left in prose.
  `long_context` carries the rates that apply above an input size, and both
  MiniMax entries use it for M3 — a vendor that prices in bands is transcribed
  now, not selected. What still has no field is a subscription's monthly figure,
  so the entries that carry one carry it in `desc`, and any per-request multiplier
  (MiniMax's `service_tier: priority` is 1.5x standard). Those stay as the marker
  of the gaps
- one rate is **published nowhere**, which is a different kind of gap. Alibaba
  excepts `qwen3.8-max` and `qwen3.8-flash` from every cache percentage it states
  — 20% is the rule for its hosted models *except* these, and 10% is the
  explicit-cache hit rate and the rule for `deepseek-v4.1-flash` — and answers
  具体价格请参见百炼控制台. The entry records 1.5 against an input of 12 (12.5%),
  which is neither, and 12.5% may well be exactly what the console says. Left
  alone rather than corrected to a percentage that does not apply to it
- `billing` says what the vendor sells **at that address**. Where one host answers
  to both an API key and a subscription credential it is `both` — the Anthropic API
  and Pro/Max, Zhipu's `api` and its coding plan — and two entries are right only
  when the addresses differ, the way `kimi` and `kimi-for-coding` do
- a plan-billed entry says whether its usage can be read with the API key alone,
  and `plan_query` names the right template when it can
- `desc` introduces the vendor and nothing that expires — no model names, no
  capabilities, no user counts (see the README's "What `desc` is for")
- the endpoints still answer, and `website` is the vendor's own site. Probe the
  full path, not the base: a bare API base answers 404 for every entry here, which
  says nothing either way. `POST` to `/chat/completions` or `/v1/messages` and
  look for 401 — the route exists and wants a key — or 422, which means it read
  the body, or 404, which is the one that means the route is gone

Eighteen of the twenty-six checked entries can be read by script rather than by eye — since 2026-09-18 that was every priced entry, until the 2026-09-22 breadth sprint added **24 seeded (Tier C) entries from models.dev**: third-party prices, pending vendor verification, read by nothing until each one upgrades: six others were removed by the owner's scope call and have not come back — prices reachable only through models.dev (groq, fireworks, upstage), only by hand (mistral, thinkingmachines) or not at all (ai21). Cerebras went in that same sweep and returned on **2026-09-21**, the day its prices turned out to be the vendor's own; that day's `perplexity` entry is new and reads its vendor page too. **Run `node scripts/fetch-all.mjs` before reading anything** — it reads every source, prints what each would change, and decides whether a person is needed. `--write` applies, `--write --commit` applies and commits one entry per commit. The per-vendor `fetch-*.mjs` scripts still work and are the better place to read about any single vendor; `fetch-openrouter-rankings.mjs` is still the only one that decides *which* models OpenRouter carries, and with `fetch-aliyun-models.mjs` for the Alibaba model lists it is the only one needing a key.

Eight entries are read by eye, and all eight by design rather than by failure: the token plans and `kimi-for-coding` publish no per-token rate at all (the runner names them every run so the absence reads as a fact, not as coverage), and the `volcesark` plans, `qianwenai-token-plan` and `xiaomi-mimo-token-plan` carry model lists that *are* scriptable — the list is the product, the price is not.

| | provider | tag | priced | website | notes |
|---|---|---|---|---|---|
| [x] | `anthropic` | official | 4/4 | <https://anthropic.com/> | checked 2026-09-14 — the four models and their prices from the vendor's own pricing page; `billing` is `both` (the API and Pro/Max share a host). Capability fields (2026-09-22) from the vendor's model overview and per-model pages on platform.claude.com: 1M/128K on Fable 5.1, Sonnet 5 and Opus 5, 200K/64K on Haiku 4.5; all four think (Fable's is always-on), use tools and structured outputs. **temperature is false on the three newer models** — the API reference states models after Claude Opus 4.6 reject any temperature but 1.0 with a 400 — while Haiku predates that and takes 0.0–1.0, forced to default while thinking is on |
| [x] | `baidu-qianfan-token-plan` | official | 0/8 | <https://cloud.baidu.com/product-s/qianfan_home> | checked 2026-09-14 — the eight models and both routes as the vendor publishes them; the openai route answers 401 without a key and the anthropic one 422 |
| [x] | `deepseek` | official | 2/2 | <https://deepseek.com/> | checked 2026-09-14 — prices, peak/off-peak, CNY, endpoints (see the fetch script). **Two footnote facts, checked 2026-09-19.** The sentence naming the peak window was **rewritten** — it read 高峰时段为北京时间… and now reads 北京时间…为高峰时段, same meaning reversed — and an adapter anchored on the old word order reported the entry unreadable the day it changed; the anchor is the timezone now, which cannot go missing. And the rule has gained a clause the schema cannot hold: peaks are Mon–Fri 不含中国法定节假日, so a Chinese public holiday is off-peak all day while the entry still bills it at peak — the same direction as the listed-rate choice, where an un-updated client overstates rather than understates. Capability fields (2026-09-22) from the pricing page (1M context, 384K max output — the API ref pins 393216) and feature table (thinking on by default, tool calls, JSON output; schema-validated output only via the Responses API); temperature is api-level and has no effect in thinking mode, which is the default |
| [x] | `volcesark` | official | 0/11 | <https://www.volcengine.com/> | checked 2026-09-14 — the eleven models the plan page names, and the two base URLs it requires are the two the entry carries. Both routes answer 401 without a key. The plan price (Lite 40 元/月, Pro 200 元/月) is published and has no field |
| [x] | `volcesark-agent-plan` | official | 0/11 | <https://www.volcengine.com/> | checked 2026-09-14 — eleven text models from the plan's own table, and the `/api/plan` base URL is confirmed by the Agent Plan's configuration doc, which also says its API key is separate from the Coding Plan's — which is why these are three entries rather than one with `billing: both`. Both routes answer 401. The tier matrix (which model each of Small/Medium/Large/Max gets) and the ladder itself (40/200/500/1000 元/月) have no field |
| [x] | `volcesark-payg` | official | 11/11 | <https://www.volcengine.com/> | checked 2026-09-14 — the eleven current-generation text models and their yuan rates from the vendor's own pricing page, which also confirmed the `doubao-seed-2.1-pro` rate removed from the Coding Plan entry. Both routes answer 401. **`long_context` cannot express four of these models** (three bands by input length); cache storage (0.017 元/百万/小时) has no field either — both recorded in place of a value. Capability fields (2026-09-22) from the vendor's model list (DocumentID 1330310): per-build context windows and max answers (1024k/256k and 256k/128k; the newest build per id — the price list's [0,1024] band matches), capability tags where 结构化输出 is a **beta** tag and absent on doubao-seed-2.0-code and the two glm rows; **doubao-seed-2.0-pro's temperature is fixed to 1 and ignored** (false), the rest api-level [0,2] |
| [x] | `kimi` | official | 4/4 | <https://moonshot.cn/> | checked 2026-09-14 — prices, currency, model list (see the fetch script). Capability fields (2026-09-22) from the pricing page's exact token counts (K3 1,048,576; the K2 pair 262,144) and the model-parameter tables: K3 thinks always and maxes output at its context; **temperature is false on all four** — the vendor pins it (K3 and K2.7-code fixed 1.0, K2.6 1.0 thinking / 0.6 otherwise) and errors on any other value — and K2.6's structured output is unstable on complex schemas |
| [x] | `kimi-for-coding` | official | 0/4 | <https://kimi.com/> | checked 2026-09-14 — model list, CNY, no per-token price (a membership carries none); `plan_query` = `kimi` |
| [x] | `stepfun` | official | 3/3 | <https://www.stepfun.com/> | checked 2026-09-18 — three flash models from the **China** pricing page (`platform.stepfun.com/…/pricing/details.md`, `api.stepfun.com/v1` 401-verified). **The markets are structured differently, not just denominated**: China bills `step-3.7-flash` at ¥1.35/¥0.27/¥8.1 with cache hits at 20% of input, while international sells the same model at $0.20/$0.04/$1.15 with hits at 10% — hence two entries, like MiniMax's pair. Re-added: the earlier removal was a sweep, not a scope ruling. **This one cannot be read from CI**: platform.stepfun.com answers GitHub's US runners with a connection failure — a Chinese host refusing foreign datacentre IPs — while platform.stepfun.ai reads fine. The scheduled run therefore reports this entry as skipped every day, by design rather than silently; its prices move only on a run from a network with a China route. Capability fields (2026-09-22) from the per-model pages (256K context; no output cap is stated anywhere — max_tokens defaults to INF) and the JSON-mode guide that names all three models (json_object/json_schema); temperature api-level 0–2, default 0.5. step-3.5-flash-2603's facts ride the shared step-3.5-flash page the vendor documents the variant on |
| [x] | `stepfun-intl` | official | 3/3 | <https://platform.stepfun.ai/> | checked 2026-09-18 — the same three models at the international market's own USD prices, from `platform.stepfun.ai/docs/en/…` (Fern markdown; `.com`'s docs drop TLS from some networks while `.ai` answers everyone — both pages are read by one adapter, each market its own object, one failing never prices the other). `api.stepfun.ai/v1` 401-verified. **models.dev priced this market by dividing the China CNY by an FX** — wrong in every column, and wrong about the discount structure too; the vendor sells these figures directly. Capability fields (2026-09-22) mirror the China arm on platform.stepfun.ai's own pages: 256K context per model page, no stated output cap, JSON mode (json_object/json_schema) naming all three, temperature api-level 0–2 default 0.5 |
| [x] | `minimax` | official | 3/3 | <https://minimaxi.com/> | checked 2026-09-14 — the three current models against the vendor's own Pay-as-you-go table, CNY, cache write included, and M3's 512k band read in both directions (≤512k 2.10/8.40/0.42, above it 4.20/16.80/0.84, which `long_context` carries); both routes answer 401 without a key. Two gaps: the same page still prices five older models this entry does not list (M2.5, M2.1, M2 and their highspeed variants), and `service_tier: priority` bills 1.5x standard with no field to record it. `billing` is `both`: one address takes a pay-as-you-go key and a Token Plan key. Capability fields (2026-09-22) from the text-generation guide and Chat Completions reference: 204,800 context on the M2.7 pair and 1,000,000 on M3 (output caps 200K / 512K via max_completion_tokens); thinking is always-on on M2.x; **structured output is genuinely absent** — the OpenAI-compatible API documents no response_format, and json_schema is scoped to a different model (MiniMax-Text-01); temperature api-level [0,2] |
| [x] | `minimax-intl` | official | 3/3 | <https://minimax.io/> | checked 2026-09-14 — the overseas table as the vendor publishes it in USD, the same three models and the same 512k band (0.60/2.40/0.12 above it); both routes answer 401. Its figures are the vendor's own round numbers rather than a conversion of the domestic ones — the two tables are published side by side, and `global.json`'s rate is for display only. Same two gaps as the domestic entry. Capability fields (2026-09-22) mirror the domestic arm on platform.minimax.io's own pages — same figures (204,800 / 1M context; 200K / 512K output caps), same absent structured output, temperature api-level [0,2] (its Responses API narrows the range to (0,1], the Chat Completions statement is the one used) |
| [x] | `google-gemini` | official | 7/7 | <https://ai.google.dev/> | checked 2026-09-17 — the seven current text models, read by `scripts/sources/google-gemini.mjs` from `aistudio.google.com/docs/pricing.md`. **The source is the markdown, and that is the whole story of this row.** The same rates are on `ai.google.dev/gemini-api/docs/pricing` as HTML, and that version cost two bugs before it was replaced — the page answers in whatever language it infers, and Italian moves the currency symbol after the number and the decimal point to a comma (`2 $`, `$0,20`, `200.000 token`), so patterns miss on some runs and not others; and its length bands are written with a literal less-than, `$2.00, prompts <= 200k tokens<br>$4.00, prompts > 200k tokens`, where a tag pattern of `<[^>]*>` runs from the `<` of `<=` to the `>` closing `<br>` and eats the first band whole. Both were the quiet kind — a plausible row, a merge comparing only the fields the proposal carries, and `no change` reported. The markdown has neither: it is byte-identical whatever language it is asked for, and has no tags to tell from arithmetic operators. **The rates are dated in advance** — every 3.x Flash reads `$0.75 through December 31, 2026, $1.50 starting January 1, 2027` — and the schema has nowhere to put a future rate, so the adapter reads whichever is in force and the switch happens on its own. `gemini-3.1-pro-preview` prices by request length and `long_context` carries it. The entry carries the OpenAI-compatible route; the native `gemini` endpoint is there for Gemini CLI. Capability fields (2026-09-22) from the per-model pages: Input token limit 1,048,576 / Output 65,536 (the vendor states input/output limits rather than one context figure) and capability tables carrying Thinking, Function calling and Structured outputs on all seven; temperature api-level [0.0, 2.0] |
| [x] | `openai` | official | 4/4 | <https://openai.com/> | checked 2026-09-17 — the four flagship text models from **OpenAI's own price list**, `developers.openai.com/api/docs/pricing.md`, read by `scripts/sources/openai.mjs`. That page serves every docs page as markdown and its Standard table's header names all eight columns — short-context input, cached input, cache writes and output, then the same four again for long context, which is `long_context` exactly. The 272K boundary is not stated in the table but is named in the rows that need it (`gpt-5.5 (<272K context length)`), so the threshold comes from there. **The vendor's list replaced a third-party reading, and showed why that mattered**: the entry had been built from `models.dev/labs/openai` because `platform.openai.com` is blocked from here, and that source lists a model called `gpt-5.6` at the same four rates as `gpt-5.6-sol` — which OpenAI does not publish. A duplicate under a name the vendor does not use, carried as a fifth flagship. It is gone. Note `platform.openai.com` and `developers.openai.com` are different hosts with different answers: the first 301s away, the second serves. Both need the proxy from here, and neither should from a US runner **Long context is a multiplier, not four more numbers**: every model page says `Prompts with more than 272K input tokens are priced at 2x input and cache rates and 1.5x output for the full request`. So `long_context.over` is 272000 and the four rates are 2×/2×/1.5× the short ones — which makes the entry's `gpt-6-astra` 20/2/25/75 off a base of 10/1/12.5/50, and the same relation holds on all four rows. `for the full request` is the part not to miss: crossing the line re-rates every token in the request, not the excess. The pricing table carries the resulting numbers, so the adapter reads those rather than re-deriving them. Note also that `gpt-5.6` is a documented **alias** for `gpt-5.6-sol` — the same model at the same rates, which is why carrying both was a duplicate rather than a fifth flagship.. Capability fields (2026-09-22) from the per-model pages' identical spec blocks: 1,050,000 context window, 128,000 max output, Reasoning token support, and Supported features carrying structured_outputs and function_calling; temperature api-level via the Responses reference, which documents no reasoning-model carve-out |
| [x] | `openrouter` | aggregate | 65/65 | <https://openrouter.ai/> | checked 2026-09-14 — model list from the vendor's own usage ranking, prices and upstream ids from its models API (see the two fetch scripts). The capability fields (2026-09-22) come off the same API: `top_provider.context_length` — the limit OpenRouter actually routes under, and on 11 rows the conservative choice over the model's nominal `context_length` — and `max_completion_tokens` for the lengths; the closed `supported_parameters` list for the flags, where absence from the list is an explicit no: 11 of the reasoning rows (gpt-5.x/6, claude) take no `temperature`, `gpt-4o-mini` and `mistral-nemo` take no `reasoning`, `hy3-preview` takes no structured output |
| [x] | `qianwenai` | official | 2/2 | <https://www.qianwenai.com/> | checked 2026-09-15 — the two models and their `in`/`out` from the vendor's own price list, read by `scripts/sources/qianwen.mjs`. **The cache rates are not published**: the context-cache page excepts `qwen3.8-max` / `qwen3.8-flash` from every percentage it gives (20% implicit, 10% explicit-hit) and says 具体价格请参见百炼控制台 — see the note below. Both routes answer without a key. Capability fields (2026-09-22) from the model-studio help pages: 100万 context on both (the billing page prices a single 0–1M band, corroborating), 3.8 series think by default, both series are on the Function Calling and json_schema/json_object lists; max output is a console-only figure → ABSENT; temperature api-level with a thinking-mode floor (values below 0.6 silently raised) |
| [x] | `qianwenai-token-plan` | official | 0/9 | <https://www.qianwenai.com/> | checked 2026-09-14 — the nine models read off the plan's own `/compatible-mode/v1/models` (see the script); the anthropic route answers 401 without a key |
| [x] | `togetherai` | official | 6/6 | <https://www.together.ai/> | checked 2026-09-18 — six models from the vendor's **own docs table** (`docs.together.ai/docs/serverless-models.md`, a Fern page serving markdown; read by `scripts/sources/togetherai.mjs`, header-named columns, and only the four-column table — the page repeats itself with a three-column version whose rows slide the output rate into the cache slot). Qwen3.7 Max doubled on the vendor since models.dev last synced (1.25→2.50); the vendor's table wins. Endpoint 401-verified. Capability fields (2026-09-22) from the serverless-models table: Context length per row (1048576 / 1048575 / 1048576 / 524288 / 131072 — qwen3.7-max is a dash on every column and appears on no capability page, so everything but api-level temperature is ABSENT), Function calling and Structured outputs columns, and the reasoning page's supported-models table (GLM-5.3 rides the recommended-models Reasoning row; the reasoning page itself lists GLM-5.2 — the disagreement is recorded, not resolved); no per-model output cap is published |
| [x] | `tencent-token-plan` | official | 0/15 | <https://www.tencent.com/> | checked 2026-09-14 — the fifteen models of both plans, which share one address and one API key; both routes answer 401 without a key |
| [x] | `xai` | official | 7/7 | <https://x.ai/> | checked 2026-09-14 — the seven models and both long-context bands read off the vendor's table; `desc` was the placeholder "Official pricing". **The endpoint is unprobed** — the local proxy was down, which is the one line of this row still outstanding. Capability fields (2026-09-22) from the per-model pages: context 500K (grok-4.5/4.6), 256K (grok-build-0.1) and 1M (the 4.20s), Function calling and Structured outputs Yes on all seven, Reasoning Yes on six and **explicitly No on grok-4.20-0309-non-reasoning** (false); no max output is stated — the reference's 128K is a request default; temperature api-level 0–2 |
| [x] | `xiaomi-mimo` | official | 5/5 | <https://mimo.mi.com/> | checked 2026-09-22 — the V2.6 lineup (Pro / Flash / Pro-UltraSpeed at up to 20×) plus the V2.5 pair, which stays live at the same prices until 2026-10-21; all from the vendor's own table (see the fetch script). The table gained an **Inference Type** column on Sep 21: the Real-time API rows are carried, and the Batch API rows — the same models at a discount for a different billed mode, offline jobs — are reported and skipped, the way the ASR rows already were, because neither has a field here. One host serves both markets and bills by the account's region, so the yuan table is a choice. **Also the pilot for the capability fields** (2026-09-22): all five carry `context`/`max_output`/`reasoning`/`tool_call`/`structured_output`/`temperature`, read off the vendor's Models overview and Model Hyperparameters pages — every model lists Deep Thinking / Function Call / Structured Output and Context 1M / Maximum Output 128K (recorded as 1000000/128000, the vendor's own figure at its decimal reading). The temperature flag carries a caveat the boolean cannot hold: in thinking mode all five force `temperature`/`top_p` back to 1.0/0.95 whatever the caller passes |
| [x] | `xiaomi-mimo-token-plan` | official | 0/4 | <https://mimo.mi.com/> | checked 2026-09-22 — the four text models it serves, unpriced: the V2.6 pair on every edition, the V2.5 pair on the individual edition only (both until 2026-10-21), and no UltraSpeed on any plan. It deducts by quota conversion, and the metered entry carries the rates |
| [x] | `zhipu-glm` | official | 2/2 | <https://bigmodel.cn/> | checked 2026-09-14 — GLM-5.3 and -Flash with their rates from the vendor's page (CNY); `billing` is `both`, and the `zhipu` quota template reads the domestic host. Capability fields (2026-09-22) from the two model pages and capability guides: 1M context / 128K max output on both; thinking is **forced** (cannot be disabled) on GLM-5.3 and -Flash; Function Calling and 结构化输出 in both models' capability lists — though the chat API's structured output is JSON-mode only (json_object, no json_schema); temperature api-level [0.0, 1.0], default 1.0 for the series |
| [x] | `zhipu-glm-intl` | official | 2/2 | <https://z.ai/> | checked 2026-09-14 — the same two models as Z.AI lists them (USD); `billing` is `both`; the same `zhipu` template, which routes to api.z.ai off the entry's own host. Capability fields (2026-09-22) mirror bigmodel.cn on docs.z.ai's own pages — 1M context / 128K max output, forced thinking on both models, Function Calling and Structured Output in the capability lists, JSON-mode-only structured output, temperature api-level [0.0, 1.0] |
| [x] | `cerebras` | official | 2/2 | <https://www.cerebras.ai/> | checked 2026-09-21 — both public models from the **vendor's own docs**, read by `scripts/sources/cerebras.mjs`: each model page carries one sentence, `Pricing: $0.35 per million input tokens, $0.75 per million output tokens`, and the words in it say which number is which, so a page that swapped the pair would swap the labels with it. **This entry was removed on 2026-09-18 as models.dev's reading and is back on the vendor's**: `cerebras.ai/pricing` is not a page without prices, it is a page whose prices are client-rendered — the served HTML carries the heading and then `<!--$!--><template data-dgst="BAILOUT_TO_CLIENT_SIDE_RENDERING">` where its tables should be — and the same figures sit in that page's Sanity payload, which the removal note itself had spotted. The docs pages state them in prose instead. Page discovery is the docs index rather than a slug map, because `gpt-oss-120b` lives at `models/openai-oss` and no rule derives one from the other. **No cache rates exist to record**: Prompt Caching is documented as a latency feature with no price attached, and the pricing page does not price it either. `api.cerebras.ai/v1` answers 401 — **through the proxy**, since direct, Cloudflare answers this network with error 1009 — and the vendor's own `curl` example uses that base. `qwen3.8-27b` is the canonical id and carries the vendor's `qwen-3.8-27b` in `serves`; the flagship is `gpt-oss-120b` **by hand**, over the highest-output rule, which would have picked the Qwen at 1.49 and said less about the vendor. models.dev carries the same two models at the same four rates, and is not the source. Capability fields (2026-09-22) from the per-model pages: context and max output are tiered free vs paid (65k/131k and 64k/128k — the qwen page states the integers outright, 131,072 and 40,960; gpt-oss-120b is transcribed at its page's own 131k/40k), Capabilities lists carrying Reasoning, Structured Outputs and Tool Calling on both (gpt-oss-120b's tool calls come with the vendor's may-call-unspecified-tools caveat), temperature documented per model |
| [x] | `perplexity` | official | 4/4 | <https://www.perplexity.ai/> | checked 2026-09-21 — the **Router** rate card from the vendor's own docs (`docs.perplexity.ai/docs/router/models.md`, Mintlify markdown; read by `scripts/sources/perplexity.mjs`, which needs the proxy from this machine and not from CI). **The entry carries the surface that outlives today** — and the only one the app can call. Perplexity's own notice retires Sonar Chat Completions on **2026-09-27**, six days out, and the Agent API answers `POST /v1/agent` (OpenAI alias `/v1/responses`), neither of which is the path the catalog's `openai` protocol composes; the Router speaks `/router/v1` for OpenAI clients and `/router` for the Anthropic SDK, which appends `/v1/messages` — the same two URL shapes `openrouter` carries, both 401-verified. **So models.dev's picture of this vendor is the one thing not followed here**: it lists the four Sonar models, none of which are these, on the API that is being retired. Two shapes to know about the page. The currency is escaped in the headers (`Input (\$/1M)`) and bare in the cells (`3.00`), so a parser expecting the escape sees no rate in either place. And **cache writes are priced by a sentence, not a column**: the note under the table says writes bill "at the rate shown (or at the input rate where no dedicated write rate is listed)", and no write rate is listed, so `cache_creation` is each row's own input rate — except `perplexity/glm-5.3-flash`, which the same note excepts from writes entirely. The adapter **requires** that sentence, so a page that stopped saying it drifts rather than reporting no change. The rates are Perplexity's own hosting rather than a markup: `glm-5.3` and `glm-5.3-flash` land on exactly Z.AI's USD figures. Flagship is `kimi-k3`, the highest output price. Two facts with no field: the Router is in **private preview** (api@perplexity.ai), and Sonar's per-request search fee — $5–$14 per 1,000 by context size on the retired surface — is not a token rate at all. Capability fields (2026-09-22) from the Router pages: per-model reasoning statements (Kimi K3, the GLM pair, Nemotron 3 Ultra all named reasoning models), and tool calling / structured outputs / temperature from the Router endpoint's Honored-parameter list — api-level, with the vendor's own warning that not every third-party model supports every feature; context and max output are stated nowhere on docs.perplexity.ai and stay ABSENT |
| [x] | `ai21` | official | 2/2 | <https://www.ai21.com/> | checked 2026-09-23 — verified against the vendor's own pricing page (www.ai21.com/pricing, server-rendered despite the docs being a React shell): Jamba Large $2/$8 and Jamba Mini $0.2/$0.4, exactly what the seed carried, so the upgrade swaps the provenance, not the numbers. Read by eye — no adapter, and the entry is no longer marked seeded |
| [ ] | `azure-openai` | official | 84/84 | <https://azure.microsoft.com/products/ai-services/openai-service> | seeded from models.dev 2026-09-22 (Tier C, unverified) — 84 priced rows, flagship `gpt-5.4-pro` by the top-output rule; prices/limits from the seed database, endpoint no endpoint yet (resource-pinned API base, waits for verification) |
| [ ] | `baseten` | official | 23/23 | <https://www.baseten.co/> | seeded from models.dev 2026-09-22 (Tier C, unverified) — 23 priced rows, flagship `moonshotai/Kimi-K3` by the top-output rule; prices/limits from the seed database, endpoint https://inference.baseten.co/v1 |
| [ ] | `bedrock` | official | 165/165 | <https://aws.amazon.com/bedrock/> | seeded from models.dev 2026-09-22 (Tier C, unverified) — 165 priced rows, flagship `anthropic.claude-opus-4-1-20250805-v1:0` by the top-output rule; prices/limits from the seed database, endpoint no endpoint yet (resource-pinned API base, waits for verification) |
| [ ] | `cloudflare-workers-ai` | official | 27/27 | <https://developers.cloudflare.com/workers-ai/> | seeded from models.dev 2026-09-22 (Tier C, unverified) — 27 priced rows, flagship `@cf/deepseek-ai/deepseek-r1-distill-qwen-32b` by the top-output rule; prices/limits from the seed database, endpoint no endpoint yet (resource-pinned API base, waits for verification) |
| [ ] | `cohere` | official | 9/9 | <https://cohere.com/> | seeded from models.dev 2026-09-22 (Tier C, unverified) — 9 priced rows, flagship `command-a-03-2025` by the top-output rule; prices/limits from the seed database, endpoint https://api.cohere.com/compatibility/v1 |
| [ ] | `databricks` | official | 30/30 | <https://www.databricks.com/> | seeded from models.dev 2026-09-22 (Tier C, unverified) — 30 priced rows, flagship `databricks-claude-opus-4-1` by the top-output rule; prices/limits from the seed database, endpoint no endpoint yet (resource-pinned API base, waits for verification) |
| [x] | `deepinfra` | official | 68/68 | <https://deepinfra.com/> | checked 2026-09-22 — **upgraded from the seed to Tier A**: prices/limits come from DeepInfra's own public models API (`api.deepinfra.com/v1/openai/models`, the JSON its catalogue page renders from), read by `scripts/sources/deepinfra.mjs`; `input_tokens`/`output_tokens` are the token pair, `cache_read_tokens` rides beside it, and rows priced by another measure (audio seconds, images, embeddings — 85 of them) are reported and skipped. The API's per-token floats arrive with binary artefacts and are rounded to the vendor's own six-decimal display precision. The catalogue rotates freely: the seed's MiniMax-M2.5/M2.7, Kimi-K2.5, GLM-5 and GLM-4.7-Flash are gone from the vendor's list, and the live list adds hosted claude/gemini/gpt-oss rows models.dev had not heard of — the vendor's live list is the one the entry follows |
| [ ] | `digitalocean` | official | 84/84 | <https://www.digitalocean.com/> | seeded from models.dev 2026-09-22 (Tier C, unverified) — 84 priced rows, flagship `openai-gpt-5.4-pro` by the top-output rule; prices/limits from the seed database, endpoint https://inference.do-ai.run/v1 |
| [ ] | `fireworks` | official | 33/33 | <https://fireworks.ai/> | seeded from models.dev 2026-09-22 (Tier C, unverified) — 33 priced rows, flagship `accounts/fireworks/routers/kimi-fast-latest` by the top-output rule; prices/limits from the seed database, endpoint https://api.fireworks.ai/inference/v1 |
| [x] | `friendli` | official | 7/7 | <https://friendli.ai/> | checked 2026-09-22 — **upgraded from the seed to Tier A**: prices come from the vendor's public pricing feed (`friendli.ai/api/public/model-apis` — the JSON the docs' pricing page itself renders from), read by `scripts/sources/friendli.mjs`. Every rate carries a discountRate and the page displays the **adjusted** price, so the adapter reproduces the page's own formula (`amount * (1 - discountRate)`, rounded to six decimals) — a listed 1.4 at 10% off is billed 1.26 and that is what the entry carries. The seed matched the vendor feed row for row; the upgrade swaps the source, not the numbers |
| [ ] | `groq` | official | 9/9 | <https://groq.com/> | seeded from models.dev 2026-09-22 (Tier C, unverified) — 9 priced rows, flagship `qwen/qwen3.8-27b` by the top-output rule; prices/limits from the seed database, endpoint https://api.groq.com/openai/v1 |
| [ ] | `huggingface` | official | 77/77 | <https://huggingface.co/> | seeded from models.dev 2026-09-22 (Tier C, unverified) — 77 priced rows, flagship `moonshotai/Kimi-K3` by the top-output rule; prices/limits from the seed database, endpoint https://router.huggingface.co/v1 |
| [ ] | `mistral` | official | 32/32 | <https://mistral.ai/> | seeded from models.dev 2026-09-22 (Tier C, unverified) — 32 priced rows, flagship `mistral-medium-2604` by the top-output rule; prices/limits from the seed database, endpoint https://api.mistral.ai/v1 |
| [ ] | `nebius` | official | 20/20 | <https://nebius.com/> | seeded from models.dev 2026-09-22 (Tier C, unverified) — 20 priced rows, flagship `moonshotai/Kimi-K3` by the top-output rule; prices/limits from the seed database, endpoint https://studio.nebius.com/v1 |
| [x] | `novita` | official | 107/107 | <https://novita.ai/> | checked 2026-09-22 — **upgraded from the seed to Tier A**: prices/limits come from Novita's own models API (`api.novita.ai/v3/openai/models`, keyless), read by `scripts/sources/novita.mjs`; the API prices in two shapes and the vendor's own decimal spelling (`price_per_m_decimal: "0.15"`) is the one transcribed, not the integer beside it. `features` is the vendor's own capability list, so reasoning/tool_call/structured_output are true only, exactly as the API says them |
| [ ] | `oci` | official | 9/9 | <https://www.oracle.com/cloud/ai/> | seeded from models.dev 2026-09-22 (Tier C, unverified) — 9 priced rows, flagship `xai.grok-4.6` by the top-output rule; prices/limits from the seed database, endpoint no endpoint yet (resource-pinned API base, waits for verification) |
| [ ] | `ovhcloud` | official | 12/12 | <https://www.ovhcloud.com/en/public-cloud/ai-endpoints/> | seeded from models.dev 2026-09-22 (Tier C, unverified) — 12 priced rows, flagship `qwen3.5-397b-a17b` by the top-output rule; prices/limits from the seed database, endpoint https://oai.endpoints.kepler.ai.cloud.ovh.net/v1 |
| [ ] | `sap-ai-core` | official | 45/45 | <https://help.sap.com/docs/sap-ai-core> | seeded from models.dev 2026-09-22 (Tier C, unverified) — 45 priced rows, flagship `anthropic--claude-3-opus` by the top-output rule; prices/limits from the seed database, endpoint no endpoint yet (resource-pinned API base, waits for verification) |
| [ ] | `siliconflow` | official | 57/57 | <https://siliconflow.com/> | seeded from models.dev 2026-09-22 (Tier C, unverified) — 57 priced rows, flagship `moonshotai/Kimi-K3` by the top-output rule; prices/limits from the seed database, endpoint https://api.siliconflow.com/v1 |
| [ ] | `siliconflow-cn` | official | 41/41 | <https://siliconflow.cn/> | seeded from models.dev 2026-09-22 (Tier C, unverified) — 41 priced rows, flagship `Pro/zai-org/GLM-5.1` by the top-output rule; prices/limits from the seed database, endpoint https://api.siliconflow.cn/v1 |
| [x] | `upstage` | official | 5/5 | <https://www.upstage.ai/> | checked 2026-09-23 — verified against the vendor's own pricing page, which **corrected two seeded rows**: solar-pro2 and solar-pro3 are 0.15 in / 0.60 out with cache reads at 0.015 (the seed carried 0.25/0.25 and no cache), and **solar-mini4 is new** — the launch of the month, $0.10/$0.40 with a $0.01 cache read (its id follows the vendor's own undated pattern, solar-pro4 → solar-mini4, inferred rather than API-verified). The page also runs dated launch promos — Solar Pro 4 at 70% off through 2026-10-09, Solar Mini 4 at 50% off through 2026-10-22 — which is a published discount with a schedule, so it rides as a news item while the rows keep the standard rates the promos return to |
| [ ] | `vertex` | official | 50/50 | <https://cloud.google.com/vertex-ai> | seeded from models.dev 2026-09-22 (Tier C, unverified) — 50 priced rows, flagship `gemini-3-pro-image` by the top-output rule; prices/limits from the seed database, endpoint https://aiplatform.googleapis.com/v1 |
| [ ] | `watsonx` | official | 5/5 | <https://www.ibm.com/products/watsonx-ai> | seeded from models.dev 2026-09-22 (Tier C, unverified) — 5 priced rows, flagship `meta-llama/llama-4-maverick-17b-128e-instruct-fp8` by the top-output rule; prices/limits from the seed database, endpoint no endpoint yet (resource-pinned API base, waits for verification) |

Priced counts are `priced/total` models. The list is a snapshot of `entries/` — a new provider needs a new line, and a removed one loses its own.
